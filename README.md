A small skeleton for Express (backend for frontend's porpuse).

Routes and controllers are set up, as well as middleware: validation of the id field and returning errors as JSON. Requests are sent to https://jsonplaceholder.typicode.com/

Used Bruno for api test (`src/bruno`).
